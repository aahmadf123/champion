{
  "name": "champions-complex",
  "version": "1.0.0",
  "private": true,
  "description": "Champions Complex fundraising page for University of Toledo Athletics. Builds a standalone GitHub Pages site and a Sidearm CMS feature-page fragment from one source.",
  "type": "module",
  "scripts": {
    "images": "node scripts/images.mjs",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "a11y": "node scripts/a11y.mjs",
    "shots": "node scripts/shots.mjs",
    "sections": "node scripts/sections.mjs",
    "sidearm-host": "node scripts/sidearm-host.mjs",
    "all": "npm run images && npm run build && npm run check",
    "verify": "npm run check && npm run sidearm-host && npm run a11y && npm run shots"
  },
  "devDependencies": {
    "playwright-core": "^1.62.0",
    "sharp": "^0.34.5"
  }
}
