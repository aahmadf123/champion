{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Open the built page (run: npm run all, then npm run serve)",
      "url": "http://127.0.0.1:8420/index.html",
      "webRoot": "${workspaceFolder}/dist"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "Open the concept chooser",
      "url": "http://127.0.0.1:8420/preview/",
      "webRoot": "${workspaceFolder}/dist"
    }
  ]
}
